export const metadata ={
    title: "Usuarios - CMD PAMI"
}

export default function UsuariosLayout({ children}) {
    return (
        <div className="w-full flex flex-col items-center">
            {children}
        </div>
    )
}